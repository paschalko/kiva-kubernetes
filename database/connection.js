/**
 * MongoDB connection is configured in backend/src/config/database.js
 * and uses MONGODB_URI from backend/.env
 *
 * This folder is reserved for database-related config and scripts.
 * Create your MongoDB database (local or Atlas) and set MONGODB_URI in backend/.env
 */

module.exports = {};
